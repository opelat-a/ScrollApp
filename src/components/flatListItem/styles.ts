import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    view: {
        width: '100%',
        marginVertical: 5,
        flexDirection: 'row',
        minHeight: 90,
        paddingHorizontal: 10,
    },
});

